# port | Host header | expected marker (regex)
# Pick a marker only your app emits. "Welcome to nginx" is not a marker.
3000 | www.example.com | Example Widgets Ltd
3001 | api.example.com | "status"[[:space:]]*:[[:space:]]*"ok"
